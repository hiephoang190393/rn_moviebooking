import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        flexDirection: 'row',
        justifyContent: 'center',
        flex: 1,
    },
    disabled: {
        opacity: 0.3,
    },
    text: {
        textAlign: 'center',
        padding: 4,
    },
});
